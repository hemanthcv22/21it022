import axios from "axios";

const BASE_URL = "http://20.244.56.144/test/companies/";

export const useFetch = (company, category, top, minPrice, maxPrice) => {
  const [data, setData] = useState(null);
};
